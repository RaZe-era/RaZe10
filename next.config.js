/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // You might need to add image domains here if using external images
  // images: {
  //   domains: ['example.com'],
  // },
}

module.exports = nextConfig
